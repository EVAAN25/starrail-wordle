# 接手说明 · 猜！星！铁！（星穹铁道每日猜角色）

## 现状（2026-08-05 收尾）

- **已上线**：https://evaan25.github.io/starrail-wordle/ （GitHub Pages，仓库 github.com/EVAAN25/starrail-wordle，个人号 public）
- 纯静态无后端：每日答案=日期种子前端确定性计算，无数据库无 API，流量全走 GitHub CDN，成本恒为零。
- 三个模式：每日挑战（六维属性比对）、无限模式、像素立绘（**独立玩法**：独立种子/存储/分享卡，2026-08-05 应用户要求从"联动主游戏"拆出）。
- 数据：85 名角色，性别/实装版本抓自 BWIKI API（非手填）；图标+立绘全部本地化（7.3MB 总量）。
- 自测：`node test.js` 18 项全过。

## 目录结构

- `index.html / style.css / app.js` — 页面与 UI 层
- `game.js` — 纯逻辑（node 可 require），顶部 `SITE_URL` 换域名时改这里
- `data/characters.json(.js)` + `tools/fetch_wiki.py / build_data.py` — 可重跑的数据管线
- `test.js` — 自测；`README.md` 部署说明；`task.md` 需求与交付记录

## 更新与部署

改完直接 `git commit && git push`，GitHub Pages 约 1 分钟自动重建（`gh api repos/EVAAN25/starrail-wordle/pages/builds?per_page=1` 查状态）。

## 开放项 / 下次继续

1. **冷启动推广未做**：NGA/B站/微博超话发帖、找主播闭卷直播玩（猜角色是现成直播节目）。
2. 新角色随版本上线后需重跑 tools 管线更新题库（注意 1224 巡猎三月七的重定向修正，见 data/DATA_SOURCES.md）。
3. 候选新玩法（用户已表达兴趣，见下个项目）：语音猜人、Higher-Lower 人气对决、阵营连线等。
4. 国内访问 github.io 需代理；如正式推广建议套自有域名+CDN。
5. "超越 x% 玩家"为本地估算（页面已注明），暂无真实统计后端（刻意保持纯静态）。

## 坑

- 本机网络对 github.io / github.com 网页域名 TLS 握手会被重置，验证线上只能看 Pages API 的 build 状态（git over SSH 与 api.github.com 正常）。
- `file://` 双击可跑靠 `data/characters.js` 内嵌数据，改 characters.json 后要同步重生成（tools 管线会处理）。
